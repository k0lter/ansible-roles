{% if ansible_prolog -%}
{% from 'templates/ansible/prolog.j2' import prolog with context %}
{{ prolog() }}
{% endif -%}
deb {{ debian_mirror }} jessie main contrib non-free
#deb-src {{ debian_mirror }} jessie main contrib non-free

deb {{ debian_mirror }} jessie-updates main contrib non-free
#deb-src {{ debian_mirror }} jessie-updates main contrib non-free

deb http://security.debian.org/ jessie/updates main contrib non-free
#deb-src http://security.debian.org/ jessie/updates main contrib non-free

deb {{ debian_mirror }} jessie-backports main contrib non-free
#deb-src {{ debian_mirror }} jessie-backports main contrib non-free
