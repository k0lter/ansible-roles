- include_tasks: zabbix.yml
  when: with_zabbix_agent
  tags:
    - 'monitoring'
    - 'zabbix'
- include_tasks: mon.yml
  when: with_mon
  tags:
    - 'monitoring'
    - 'mon'

# vim: ft=yaml.ansible
