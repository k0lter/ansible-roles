- name: 'Reload apache2'
  shell:
    cmd: apache2ctl configtest && apache2ctl graceful
  check_mode: no

- name: 'Restart apache2'
  service:
    name: 'apache2'
    state: 'restarted'

# vim: ft=yaml.ansible
